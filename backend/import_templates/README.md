# Import templates

Hand-formatted Excel templates go in this folder as `<slug>-template.xlsx`.

The full guide — file names for every grid, formatting rules and how the
out-of-date check works — is in `Import-Excel-Template-Readme.md` at the repo root.
